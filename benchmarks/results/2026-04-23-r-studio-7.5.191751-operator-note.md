## R-Studio for Mac operator note

Date: `2026-04-23`
Tool: `R-Studio for Mac`
Version: `7.5.191751`
Scope attempted: `signature_carving_jpeg_v1`
Status: `blocked-before-run`, not a benchmark run

### Evidence captured

- operator note from the previous pass: `benchmarks/results/2026-04-22-r-studio-7.5.191751-operator-note.md`
- sample trace on a user-launched app copy: `/tmp/rs-intact-user.sample.txt`
- SecurityAgent prompt capture: `/tmp/rstudio-window-captures/securityagent-340.png`
- later SecurityAgent captures during a repeated launch:
  - `/tmp/rstudio-window-captures/securityagent-371.png`
  - `/tmp/rstudio-window-captures/securityagent-374.png`
  - `/tmp/rstudio-window-captures/securityagent-375.png`
  - `/tmp/rstudio-window-captures/securityagent-376.png`

### What was attempted

1. Re-check the fresh-launch path after the previous GUI automation work had already proven:
   - `Demo` could be pressed;
   - `Device view` could be reached;
   - `Drive > Open Image...` could be invoked.
2. Inspect the blocked user-launched process with `sample`.
3. Enumerate on-screen windows through `CGWindowList`.
4. Inspect `SecurityAgent` through the Accessibility API and capture the visible prompt.
5. Test a local copied bundle instead of the mounted app.
6. Test variants with `rauthd`, `RDriver.kext`, or both disabled in a temporary app copy.
7. Re-check embedded help / strings / startup config hints:
   - `-help` output;
   - `test_dev_settings.ini`;
   - embedded strings around admin rights and deleted-file processing.
8. Re-test the startup path with a temporary `HOME` and a synthetic `test_dev_settings.ini`.
9. Re-run the mounted app path and explicitly cancel all concurrent `SecurityAgent` prompts.

### Outcome

- the central blocker is now sharper than before:
  - a fresh user-launched app copy stalls in `CheckForAdministrativePrivileges(bool)`;
  - the sampled main thread is blocked in `AuthorizationCreate`;
  - this is visible in `/tmp/rs-intact-user.sample.txt`.
- the prompt is not theoretical:
  - `SecurityAgent` shows a real password dialog for `R-Studio`;
  - the dialog contains two text fields and `Annuler` / `OK`;
  - one captured prompt explicitly says that `R-Studio` wants to perform changes and requests the password to authorize the operation.
- the mounted-app path can now spawn multiple concurrent `SecurityAgent` prompts:
  - four windows were visible at once in one replayed launch.
- cancelling the prompt does not fall through to a useful degraded mode in this session:
  - all visible `SecurityAgent` cancel buttons were pressed successfully with `AXPress`;
  - the prompts disappeared;
  - no `R-Studio` benchmarkable window replaced them;
  - the launch flow effectively died there.
- disabling `rauthd` and/or `RDriver.kext` in a temporary app copy changes the surface, but does not produce a runnable benchmark path:
  - direct executable launches stay alive for a short interval;
  - no visible `R-Studio` window appears;
  - when sampled, the intact copied app is still blocked in `AuthorizationCreate`.
- no documented headless or CLI image-loading path was confirmed:
  - `-help` exposes only general startup/debug options;
  - no switch was found that cleanly bypasses the admin gate for an image-only benchmark flow.
- the embedded `test_dev_settings.ini` mechanism is real, but no verified bypass was obtained from it in this session:
  - the template comments and keys are present in the binary;
  - a temporary `HOME` plus synthetic `test_dev_settings.ini` did not yield a proven admin-bypass or a benchmarkable UI path.
- no image was opened, no scan was completed, and no benchmark JSON was produced.

### Practical blocker

The remaining blocker is no longer best described as "GUI automation is hard". In this environment, `R-Studio for Mac 7.5.191751` is now evidenced to block earlier, at the administrative-rights gate itself: the startup flow enters `CheckForAdministrativePrivileges(bool)`, then waits in `AuthorizationCreate`, and cancelling the system prompts terminates the useful path instead of yielding an image-only degraded mode. A real `R-Studio` comparative run still requires either valid administrator authorization on the machine, or another verified non-admin startup path that has not been demonstrated here.
